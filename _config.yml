title: Your Site
markdown: kramdown
theme: jekyll-theme-primer

# Keep Jekyll away from build artifacts and package folders
exclude:
  - node_modules
  - backend/node_modules
  - vendor
  - package.json
  - package-lock.json
  - yarn.lock
  - pnpm-lock.yaml
  - webpack.config.js
  - vite.config.*
  - README.md  # if your repo README shouldn’t become a page
